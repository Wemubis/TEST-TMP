---
conteneurs:
  hosts:
    machineB:
      ansible_host: 10.150.36.140
    machineC:
      ansible_host: 10.150.36.141
  vars:
    ansible_user: root
    # Clé utilisée par Ansible. Reporter le même chemin dans ssh_opts
    # (scan.yml) pour que le rapatriement utilise la même.
    ansible_ssh_private_key_file: ~/.ssh/vulnscan
